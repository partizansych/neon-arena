using Godot;

public struct Position { public Vector2 Value; }
public struct Velocity { public Vector2 Value; }
public struct Speed	{ public float Value; }
public struct GodotRef { public Node2D Value; }

// Маркер для быстрого поиска игрока
public struct IsPlayer { }